# 《无名之界》

一个由 LLM 驱动的命令行神明模拟器。你扮演一个正在注视世界的存在，从石器时代开始，见证文明的生长与消亡。

---

## 世界观

这个世界建立在两种根本底质之上：**凝质（Sang）**，构成一切有形之物；**息（Veth）**，渗透万物的意识场域。两者并非对立，而是同一实在的两面。科技与魔法在这里不是分类，而是同一力量的不同读取方式。

世界从石器时代起步。没有可参考的历史，没有已知的神话。一切都在发生之中。

---

## 特性

- **每个命名居民都有完整的个人时间线** — 出生、相遇、变异、祈祷、死亡，全部记录
- **真实流逝的世界时间** — 后台线程每 5 分钟推进 1 年；离线期间时间也在流逝，回来时自动补偿
- **三级变异系统** — 表层 / 功能 / 本质变异，可扩散感染周围的人、物、地形，全部自主发生
- **NPC 自主权** — 居民有自己的信仰、祈祷、记忆碎片，以及向神明提出改变世界的提案
- **神明传话** — 直接向某人传达意志；对方是否感知取决于信仰深度和自身资质
- **神明自询** — 以你所见为据回溯世界脉络，未曾见过的无法回答
- **世界故事** — 随时查看当下正在发生的事：居民在想什么、做什么，大事小事皆可见
- **自生长法则** — 游戏检测到新兴系统时，LLM 自动生成对应 Python 模块并热加载；崩溃自动修复
- **世界神话库** — 重大事件自动沉淀为神话，流传于居民口中
- **别的神明** — 你不是唯一的存在

---

## 安装

```bash
pip install openai python-dotenv rich
```

复制 `.env.example` 为 `.env`，填入你的 [OpenRouter](https://openrouter.ai) API Key：

```
OPENROUTER_API_KEY=sk-or-v1-你的key
```

---

## 启动

```bash
python main.py
```

首次运行自动创世。之后每次启动读取存档，世界继续。

---

## 指令

支持中英文双输入。

| 指令 | 英文 | 说明 |
|------|------|------|
| `赐予 <物品>` | `gift` | 消耗 10 神力，向部落赐予一件物品（赐予后部落需时间消化） |
| `施放 <奇迹>` | `cast` | 消耗 50 神力，降下神迹 |
| `传话 <名字> <内容>` | `talk` | 向某人传达神明意志 |
| `回应 <名字> <类型>` | `respond` | 回应祈祷：答应 / 无视 / 惩戒 / 赐福 |
| `凝视 <名字或地点>` | `gaze` | 深度洞察某人或某地 |
| `问 <问题>` | `ask` | 神明自询，以所见为据回溯世界脉络 |
| `故事` | `story` | 查看当下正在发生的世界切片 |
| `状态` | `status` / `s` | 世界概况（神力、人口、变异、法则） |
| `地图` | `map` / `m` | 世界感知图 |
| `人群` | `people` | 命名居民列表 |
| `人 <名字>` | `person` | 某人的完整人生时间线 |
| `人物` | `entities` | 特殊人物 |
| `神话` | `myths` | 世界神话库 |
| `祈祷` | `prayers` | 等待回应的祈祷列表 |
| `法则` | `weave` | 世界自然涌现的法则 |
| `帮助` | `help` | 显示指令列表 |
| `退出` | `quit` | 离开（世界继续流逝） |

---

## 技术栈

- Python 3.11+
- OpenRouter API（claude-haiku-4-5 日常调用，claude-sonnet-4-5 代码生成）
- `rich` 终端可视化
- `threading` 后台世界模拟线程
- `exec()` 热加载 LLM 生成的法则模块
